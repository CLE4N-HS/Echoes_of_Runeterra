#pragma once
#include "Window.h"
#include "Database.h"

class DatabaseManager
{
public:
	DatabaseManager();
	~DatabaseManager();

	//template<typename T>
	//T* getValue(std::string _name);

private:

};
